package djson

import "errors"

// Encode takes a JSON Value object and generate the equivalent JSON encoded
// in UTF-8
func (v *Value) Encode() ([]byte, error) {
	return nil, errors.New("Not yet implemented")
}
